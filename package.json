{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build:frontend:watch": "npm --prefix frontend run build -- --watch --emptyOutDir false",
    "start:backend": "npm --prefix backend start",
    "dev": "node scripts/dev-single-server.js",
    "dev:single": "node scripts/dev-single-server.js",
    "start": "node scripts/dev-single-server.js"
  },
  "dependencies": {
    "lucide-react": "^1.8.0"
  }
}
